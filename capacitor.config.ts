import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.taiga72.calorietracker',
  appName: 'Calorie Tracker',
  webDir: 'dist'
};

export default config;
